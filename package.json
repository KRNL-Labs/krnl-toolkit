{
  "name": "krnl-toolkit",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "smart-contracts/hardhat"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hh": "npm install --workspace=hardhat",
    "deploy": "npm run deploy --workspace=hardhat",
    "register": "npm run register --workspace=hardhat",
    "full-deploy": "npm run dvr --workspace=hardhat"
  }
}
